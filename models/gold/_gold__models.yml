version: 2

models:
  - name: mart_orders_detailed
    description: "Gold layer consumption-ready One Big Table"
    columns:
      - name: order_pk
        tests:
          - unique
          - not_null
      - name: region_name
        tests:
          - not_null